server:
  port: 8080

spring:
  application:
    name: springboot-dubbo-consumer

dubbo:
  application:
    name: springboot-dubbo-consumer
  registry:
    protocol: zookeeper
    address: 127.0.0.1:2181